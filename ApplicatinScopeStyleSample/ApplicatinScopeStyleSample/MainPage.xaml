<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ApplicatinScopeStyleSample.MainPage"
             Style="{StaticResource contentPageStyle}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="localButtonStyle" TargetType="Button" BasedOn="{StaticResource buttonStyle}">
                <Setter Property="HorizontalOptions" Value="Fill"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout Padding="32">
        <Button x:Name="button1" Text="ボタン１"  Style="{StaticResource localButtonStyle}"/>
    </StackLayout>

</ContentPage>
